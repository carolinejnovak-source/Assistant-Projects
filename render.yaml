services:
  - type: web
    name: assistant-projects
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    plan: starter
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
